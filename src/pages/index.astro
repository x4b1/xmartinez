---
import BaseLayout from "../layouts/BaseLayout.astro";

import Header from "../components/cv/Header.astro";
import Experience from "../components/cv/Experience.astro";
import Projects from "../components/cv/Projects.astro";
import Download from "../components/Download.astro";

import { info } from "../content/_cv";

function cvFileName(name: string): string {
  return `${name.toLowerCase().replaceAll(" ", "_")}.pdf`;
}
---

<BaseLayout title={info.name}>
  <div class="grid min-h-screen place-items-center bg-indigo-50 print:min-h-0">
    <main
      class="m-0 bg-white text-xs shadow-md md:m-4 md:w-[210mm] print:m-0 print:h-[297mm] print:w-screen print:rounded-none print:shadow-none"
    >
      <div class="fixed top-6 right-4 md:absolute">
        <Download href={cvFileName(info.name)} />
      </div>
      <div class="flex h-full flex-col md:flex-row print:flex-row">
        <div class="md:w-1/3 print:w-1/3">
          <Header
            education={info.education}
            website={info.website}
            email={info.email}
            github={info.github}
            jobDescription={info.jobDescription}
            linkedin={info.linkedin}
            location={info.location}
            name={info.name}
            languages={info.languages}
          />
        </div>
        <div class="flex h-full flex-col space-y-4 p-4 md:w-2/3 print:w-2/3">
          <section>
            {info.about.map((about) => <p>{about}</p>)}
          </section>
          <Experience experience={info.experience} website={info.website} />
          <Projects projects={info.projects} />
        </div>
      </div>
    </main>
  </div>
</BaseLayout>
